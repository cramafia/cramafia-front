export enum LobbyGameType {
  CLASSIC = 'CLASSIC',
  CUSTOM = 'CUSTOM',
}

export enum LobbyGamePrivacy {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
}

export enum LobbyGameStatus {
  WAITING_FOR_PLAYERS = 'WAITING_FOR_PLAYERS',
  GAME_IN_PROGRESS = 'GAME_IN_PROGRESS',
}
